# Dependencies are declared in pyproject.toml — this file remains for tooling
# that doesn't yet read PEP 621 metadata.
#
#   pip install .            # runtime only
#   pip install -e .[dev]    # editable + dev tools
#
# See pyproject.toml for the canonical dependency list.
.
